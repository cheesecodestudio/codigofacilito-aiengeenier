[project]
name = "cf-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "cheesecodestudio", email = "cheesecodestudio@gmail.com" }
]
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]

[project.scripts]
cf-client = "cf_client:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
